{
  "name": "humidity-tracker",
  "version": "0.3.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "tsc && cmake . && make",
    "start": "DIR=$(pwd) (crontab -l ; echo '*/30 * * * bash cd $DIR && node index.js') | crontab -"
  },
  "repository": {
    "type": "git",
    "url": "git@ssh.dflyboy.me:daniel/humidity-tracker.git"
  },
  "author": "daniel@dpulm.online",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "i2c-bus": "^5.2.2",
    "ioredis": "^4.28.5"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/ioredis": "^4.28.8",
    "typescript": "^4.5.5"
  }
}
